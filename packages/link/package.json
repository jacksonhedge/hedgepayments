{
  "name": "@hedge/link",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build --mode loader && vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "preact": "^10.22.0"
  },
  "devDependencies": {
    "vite": "^5.3.0",
    "vitest": "^1.6.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.4.0"
  }
}
